version: "3.9"
services:
  api:
    build: .
    container_name: prompt-injection-detector
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./models:/app/models
    env_file: .env
    restart: unless-stopped
